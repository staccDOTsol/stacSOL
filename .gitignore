# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build artifacts & deps
node_modules
dist
dist-ssr

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only state
*.local
.vercel
.claude

# Secrets — every flavor of env file. .env.example IS tracked so devs
# know what to populate; everything else stays local.
.env
.env.*
!.env.example

# token-watch local dbs
tools/ansem-watch/*.db*

# ansem-aggr: vendored clone of Tucsky/aggr (has its own git)
tools/ansem-aggr/

# Wallet keypairs / authority secrets — never commit these.
keys/
*.keypair.json
*-keypair.json
secret*.json
.env*.local
.env*
