app = "megamine"
primary_region = "iad"

[http_service]
  internal_port = 4700
  force_https = true
  # bots + rounds are the show — keep the mine alive
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
